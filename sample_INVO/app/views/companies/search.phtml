<?php use Phalcon\Tag; ?>

<?php echo $this->getContent(); ?>

<table width="100%">
    <tr>
        <td align="left">
            <?php echo $this->tag->linkTo(array("index", "Go Back")); ?>
        </td>
        <td align="right">
            <?php echo $this->tag->linkTo(array("new", "Create ")); ?>
        </td>
    </tr>
</table>

<table class="browse" align="center">
    <thead>
        <tr>
            <th>Id</th>
            <th>Name</th>
         </tr>
    </thead>
    <tbody>
    <?php foreach ($page->items as $companie) { ?>
        <tr>
            <td><?php echo $companie->id ?></td>
            <td><?php echo $companie->name ?></td>
            <td><?php echo $this->tag->linkTo(array("edit/" . $companie->id, "Edit")); ?></td>
            <td><?php echo $this->tag->linkTo(array("delete/" . $companie->id, "Delete")); ?></td>
        </tr>
    <?php } ?>
    </tbody>
    <tbody>
        <tr>
            <td colspan="2" align="right">
                <table align="center">
                    <tr>
                        <td><?php echo $this->tag->linkTo("search", "First") ?></td>
                        <td><?php echo $this->tag->linkTo("search?page=" . $page->before, "Previous") ?></td>
                        <td><?php echo $this->tag->linkTo("search?page=" . $page->next, "Next") ?></td>
                        <td><?php echo $this->tag->linkTo("search?page=" . $page->last, "Last") ?></td>
                        <td><?php echo $page->current, "/", $page->total_pages ?></td>
                    </tr>
                </table>
            </td>
        </tr>
    </tbody>
</table>
